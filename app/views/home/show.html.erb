<%= render "header", query: ""%>


<h2 style="display:inline; margin-right:50px;"><%=@product.title%></h2>
 <h3 style="display:inline"> Price: &#8377 <%=@product.price%> </h3>
  <% if @product.quantity == 0 %>
    <h4 style="color:red">Out Of Stock<h4>
  <%end%>
  <h2>
  <%=@product.description%>
  </h2>
  <!-- TODO update link path correctly--> 
  <%= button_to "Add to Cart", new_user_session_path, method: :get %>
  <%= button_to "Buy Now", new_user_session_path, method: :get %>